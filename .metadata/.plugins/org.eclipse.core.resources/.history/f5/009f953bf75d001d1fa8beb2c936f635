/*
 * container.cpp
 *
 *  Created on: 6 Nov 2022
 *      Author: student
 */

#include "Container.h"
#include <stdio.h>
#include <cstring>
#include "Container.h"

Container::Container()
{
	m_counter = 0;
	memset(m_CliCommand, NULL, sizeof(CliCommand)* MAX_CLI);
}

Container::~Container()
{
	for (int i = 0; i < MAX_CLI; i++) {
		if (m_CliCommand[i] != NULL) {
			delete m_CliCommand[i];
		}
	}
}

void Container::add(CliCommand* pCliCommand)
{
	if (m_counter < MAX_CLI) {
		m_CliCommand[m_counter] = pCliCommand;
		m_counter++;
	}
}


