

#ifndef INC_CLOCK_H_
#define INC_CLOCK_H_

typedef struct _clock
{
	GPIO_TypeDef* GPIOx;
	uint16_t GPIO_Pin;
	int tick;
	int seconds;
	int minutes;


}CLOCK;

void clockInit(CLOCK* clock);
void clockOn(CLOCK* clock);
void clockOff(CLOCK* clock);


#endif /* INC_CLOCK_H_ */
